<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand">Zone téléchargement</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_film' ? 'active' }}" href={{ path("app_film") }}>Films</a>
                </li>
                {% if app.user%}
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'app_film_add' ? 'active' }}" href={{ path("app_film_add") }}>Ajouter</a>
                    </li>
                {% endif %}
                <li class="nav-item dropdown">
                    {% if app.user %}
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.email }}</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="#">Mon compte</a>
                            <a class="dropdown-item" href={{ path("app_logout") }}>Se deconnecter</a>
                        </div>
                    {% else %}
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Mon compte</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href={{ path("app_register") }}>S'inscrire</a>
                            <a class="dropdown-item" href={{ path("app_login") }}>Se connecter</a>
                        </div>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>